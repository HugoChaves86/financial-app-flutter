import 'package:flutter/material.dart';

class ListIndexWidget extends StatelessWidget {
  const ListIndexWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
